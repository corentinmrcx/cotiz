services:
  app:
    build: .
    container_name: cotiz_app
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data
    shm_size: 512m
    restart: unless-stopped
